# Generated ZIP packages
*.zip

# Generated site-spec output artifacts (rebuild from build scripts)
*-site-spec.json

# Word docs (binary, may contain client data — use Markdown instead)
*.docx
~$*

# Extracted packages for analysis (not source templates)
package-analysis/

# Claude Code local config (keep skills + commands, exclude the rest)
.claude/settings.json
.claude/settings.local.json
.claude/*.jsonl

# macOS
.DS_Store

# Node
node_modules/
