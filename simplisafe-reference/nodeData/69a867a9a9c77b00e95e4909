{"_id":"69a867a9a9c77b00e95e4909","referenceId":"b1532eb6-b47c-43bf-be0d-1e2ee7808dbb","type":"code","extension":"@cognigy/basic-nodes","label":"Generate Code","comment":"","commentColor":"","analyticsLabel":null,"isEntryPoint":false,"isDisabled":false,"mock":{"isEnabled":false,"code":""},"localizedData":[{"config":{"code":"function generateAuthCode() {\n    const isValid = (codeStr) => {\n        for (let i = 0; i < codeStr.length - 1; i++) {\n            if (\n                codeStr[i] === codeStr[i + 1] || // No repeated digits\n                codeStr[i] === '0' ||            // No zeros\n                codeStr[i + 1] === '0'\n            ) {\n                return false;\n            }\n        }\n        return !codeStr.includes('0'); // Ensure no zeros anywhere\n    };\n\n    let authCode;\n    do {\n        authCode = Math.floor(100000 + Math.random() * 900000).toString();\n    } while (!isValid(authCode));\n\n    return authCode;\n}\n\n// Assign generated code to context\ncontext.authCode = generateAuthCode();\n\n// Create email using first initial and last name\nconst firstName = context.authenticationDetails?.firstName || '';\nconst lastName = context.authenticationDetails?.lastName || '';\nconst email = `${firstName.charAt(0).toLowerCase()}.${lastName.toLowerCase()}@cognigy.com`;\n\n// Assign email to context\ncontext.generatedEmail = email;","hasError":false,"transpiled":"var _a, _b;\nfunction generateAuthCode() {\n    const isValid = (codeStr) => {\n        for (let i = 0; i < codeStr.length - 1; i++) {\n            if (codeStr[i] === codeStr[i + 1] ||\n                codeStr[i] === '0' ||\n                codeStr[i + 1] === '0') {\n                return false;\n            }\n        }\n        return !codeStr.includes('0');\n    };\n    let authCode;\n    do {\n        authCode = Math.floor(100000 + Math.random() * 900000).toString();\n    } while (!isValid(authCode));\n    return authCode;\n}\ncontext.authCode = generateAuthCode();\nconst firstName = ((_a = context.authenticationDetails) === null || _a === void 0 ? void 0 : _a.firstName) || '';\nconst lastName = ((_b = context.authenticationDetails) === null || _b === void 0 ? void 0 : _b.lastName) || '';\nconst email = `${firstName.charAt(0).toLowerCase()}.${lastName.toLowerCase()}@cognigy.com`;\ncontext.generatedEmail = email;\n"},"preview":"","localeReference":"69a867a9a9c77b00e95e48b0"}],"chartReference":"69a867a9a9c77b00e95e48b6","resourceReference":"69a867a9a9c77b00e95e48b5","projectReference":"69a867a9a9c77b00e95e484c","organisationReference":"676ec10638a57683744d287c"}